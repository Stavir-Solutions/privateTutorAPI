name: Deploy to deploy branch

on:
  push:
    branches:
      - deploy

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:

      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '16'

      - name: Create .env file
        run: |
          echo "ACCESS_KEY=${{ secrets.ACCESS_KEY }}" >> .env
          echo "SECRET_ACCESS_KEY=${{ secrets.SECRET_ACCESS_KEY }}" >> .env
          echo "REGION=us-east-1" >> .env
          echo "endpoint=${{ secrets.AWS_ENDPOINT }}" >> .env
      - name: Install dependencies
        run: npm install

      - name: Create Lambda deployment package
        run: zip -r DEVArchive.zip . -x ".git/*" -x "node_modules/*" -x ".env"

      - name: Configure AWS CLI
        uses: aws-actions/configure-aws-credentials@v3
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: us-east-1
          
      - name: Upload package to S3 and deploy to Lambda
        run: |
          aws s3 cp DEVArchive.zip s3://private-teacher-api-release/DEVArchive.zip
          aws lambda update-function-code \
            --function-name privateTutorAPIDevLambda \
            --s3-bucket private-teacher-api-release \
            --s3-key DEVArchive.zip
